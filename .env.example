SUPABASE_URL=https://ebcinozorcmwqdgmzelh.supabase.co
SUPABASE_PUBLISHABLE_KEY=
# Exact origin, no trailing path. Preview origin may also be derived from VERCEL_URL.
APP_ORIGIN=http://localhost:3000
# Enable ONLY after SMTP, templates, redirect URLs and real delivery are tested.
AUTH_EMAIL_ENABLED=false
