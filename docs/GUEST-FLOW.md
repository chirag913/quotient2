# Current delivery flow — supersedes customer-account assumptions

Deployment status (9 September 2026): the Next.js guest intake and protected CRM are live on www.skinquotient.in. Owner email chiragsharmadm@gmail.com is confirmed, owner membership is active, and password/MFA login and stored-lead visibility were verified. The original CRM design was restored in b6555e4. Migrations 001–003 are applied; production and preview settings exist but currently share one Supabase project. Password login is live at /admin; optional email-link sign-in and all payments remain disabled. Read [PROJECT-HANDOVER.md](PROJECT-HANDOVER.md) for the complete current state and verification limits.

Owner decision, 2026-09-09: customers do not sign in. Collect their name, email, WhatsApp number and assessment answers. Email is an unverified contact field, not an identity credential. WhatsApp follow-up requires the separate recorded opt-in. Do not subscribe leads to marketing automatically. Customers can open the business WhatsApp chat themselves.

Only staff sign in; staff data access still requires active membership and MFA. The guest intake uses a server-only Supabase secret to call a restricted RPC, after input validation, origin validation and server-verified Cloudflare Turnstile. Anonymous/authenticated database clients cannot invoke guest insertion or read leads. Do not expose the secret key in browser variables, source, logs or GitHub. Use environment-specific settings; isolate staging data before risky future work.

Required configuration: SUPABASE_SECRET_KEY (server-only), SUPABASE_URL, TURNSTILE_SITE_KEY, TURNSTILE_SECRET_KEY, RATE_LIMIT_SALT. Cloudflare Turnstile is separate from Email Sending. No paid email service is required to collect email addresses. Turnstile and hosted database insertion passed in preview; production configuration is enabled, while a new full post-promotion production submission remains an explicit acceptance task. Configure the widget for the exact preview and approved production hostname. Do not use test CAPTCHA keys in a public production environment.

Apply migrations 001, 002 and 003 in order. The prior authenticated assessment table remains for compatibility; the current guest flow writes to leads. Guardrails: Turnstile token validation, 16 KB body cap, strict input schema, honeypot, server/database scoring, idempotency, five submissions per contact hash per day and 500 total per day. These starter limits require operational monitoring; they are not a substitute for upstream abuse protection. No raw IP is stored. Contact hashes are keyed using a separate server secret.

Payment integration is paused. Do not add a query-string-controlled or client-controlled payment-success page. Once Razorpay is resumed, verify payment on the server and reconcile webhooks; then show a Chat with us on WhatsApp button targeting https://wa.me/919995850411. Prefilled text may include an opaque order reference; never include assessment answers, email, payment secrets or treatment data in the URL. A WhatsApp click is not a sent message, payment, booking or delivery confirmation. Staff must reconcile the reference against authoritative payment state.

The current assessment result includes a general WhatsApp contact link, not a payment-success claim. No messages are sent automatically. The owner must confirm the number is active on WhatsApp. Customer email delivery is no longer a launch dependency for this flow; any staff authentication email setup is a separate operational decision.


